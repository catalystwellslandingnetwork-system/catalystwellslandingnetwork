# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Example:
# NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9... (Keep this secret!)

# Razorpay Configuration (for payment processing)
NEXT_PUBLIC_RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret
RAZORPAY_WEBHOOK_SECRET=your-razorpay-webhook-secret

# Example:
# NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx
# RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx
# RAZORPAY_KEY_SECRET=your_secret_key_here (Keep this secret!)
# RAZORPAY_WEBHOOK_SECRET=your_webhook_secret_here (Keep this secret!)

# ============================================
# ULTRA-SECURE INTER-SERVICE COMMUNICATION
# 7-Layer Security Architecture
# ============================================

# Main Application URL (where to sync subscription data)
MAIN_APP_URL=https://app.catalystwells.com

# Layer 1: IP Whitelisting (comma-separated IPs)
ALLOWED_IPS=127.0.0.1,::1
# Production example:
# ALLOWED_IPS=123.45.67.89,98.76.54.32

# Layer 2: API Key Authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTER_SERVICE_API_SECRET=generate_64_char_hex_string
INTER_SERVICE_API_KEY_ID=landing_page_v1

# Layer 3: JWT Token Secret
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTER_SERVICE_JWT_SECRET=generate_different_64_char_hex_string

# Layer 4: Request Signing Secret
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
REQUEST_SIGNING_SECRET=generate_another_different_64_char_hex_string

# Layer 5: Data Encryption Key (AES-256, must be 32 bytes in hex = 64 chars)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DATA_ENCRYPTION_KEY=generate_32_byte_hex_key_64_chars

# ⚠️  IMPORTANT SECURITY NOTES:
# 1. Generate DIFFERENT secrets for each variable above
# 2. Use the SAME secrets in both landing page and main app .env files
# 3. Never commit .env.local to version control
# 4. Rotate these secrets every 90 days
# 5. Keep backups of these secrets in a secure password manager
